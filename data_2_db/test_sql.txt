delete from keyword where id not in (select id from (select min(id) as id from keyword group by keyword) as b);

host='www.aikepler.com',
        user='spider',
        passwd='7ujm<KI*9ol.',
        database='horizon'

host='10.10.10.2',
        user='root',
        passwd='8ik,.lo9',
        database='horizon'